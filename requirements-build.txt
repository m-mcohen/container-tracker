# Build-time environment for build.bat / tools/release.ps1 (Python 3.12).
# Runtime deps mirror pyproject.toml [project.dependencies]; pythonnet is
# pywebview's Windows backend; pyinstaller is the bundler.
requests
openpyxl
keyring
packaging>=24.2
pillow
pywebview>=5,<6
pythonnet
pyinstaller
